# doug_dock_design_min_mod2_cal_cal command file

cd %(workdir)s

[ -x %(bin)s/doug_dock_design_min_mod2_cal_cal.%(binext)s ] || exit 1

 %(bin)s/doug_dock_design_min_mod2_cal_cal.%(binext)s @flags -database %(database)s -run:constant_seed -nodelay  2>&1 \
     | egrep -v 'Finished.+in [0-9]+ seconds.' \
     | egrep -v 'Dunbrack library took .+ seconds to load' \
     | egrep -v 'core.init: command' \
     > log

test "${PIPESTATUS[0]}" != '0' && exit 1 || true
