package:
  name: rosetta

requirements:
  build:
    - python
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - cmake
    - ninja
    - hdf5

  host:
    - zlib

  run:
    - zlib
    - hdf5

test:
  commands:
    - rosetta_scripts

about:
  home: http://rosettacommons.org
