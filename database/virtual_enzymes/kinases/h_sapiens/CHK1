# Consensus               Sequence  Residue of    Atom
# Sequence                Type      CS to Modify  To Modify  Efficiency
  (I/M/L/V)X(K/R)XX(S/T)  AA        6             O          1.00        # Atom name here doesn't matter, as the Mover will simply change the VariantType

